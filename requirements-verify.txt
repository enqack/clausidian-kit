# Verification tooling dependencies (NOT project runtime deps)
# This repo template is language-agnostic; Python is used only to run the verification tools in tools/.
#
# Install:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-verify.txt
#
# Optional: enable pytest for Python projects that choose to use it.
pytest>=7.0,<9.0

# Markdown formatting
mdformat>=0.7.17
mdformat-gfm>=0.3.5
mdformat-frontmatter>=2.0.1
mdformat-footnote>=0.1.1
jsonschema>=4.17.0
